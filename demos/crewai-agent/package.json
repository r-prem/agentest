{
  "name": "crewai-agent-demo",
  "version": "1.0.0",
  "description": "Demo: testing a CrewAI (Python) agent with agentest via HTTP endpoint",
  "type": "module",
  "private": true,
  "scripts": {
    "sim": "npx agentest run",
    "serve": "cd src && python server.py"
  },
  "dependencies": {
    "@agentesting/agentest": "^0.0.5",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
